// Render the form
$('.modal-body').html('<%= j render "participants/form" %>');

// Show the dynamic dialog
$('#participant_dialog').modal("show");

// Set focus to the first element
$('#participant_dialog').on('shown.bs.modal', function () {$('#InputName').focus()})

$("#participantes").smart_listing().fadeLoaded();
