// Render the form
$('.modal-body').html('<%= j render "studio_session_types/form" %>');

// Show the dynamic dialog
$('#studio_session_type_dialog').modal("show");

// Set focus to the first element
$('#studio_session_type_dialog').on('shown.bs.modal', function () {$('#InputName').focus()})

$("#studio_session_types").smart_listing().fadeLoaded();
