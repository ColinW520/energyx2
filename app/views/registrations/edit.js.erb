// Render the form
$('.modal-body').html('<%= j render "registrations/remote_form" %>');

// Show the dynamic dialog
$('#registration_dialog').modal("show");

// Set focus to the first element
$('#registration_dialog').on('shown.bs.modal', function () {$('#InputName').focus()})

$("#registrations").smart_listing().fadeLoaded();
