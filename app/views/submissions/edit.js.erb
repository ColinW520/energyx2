// Render the form
$('.modal-body').html('<%= j render "submissions/form" %>');

// Show the dynamic dialog
$('#submission_dialog').modal("show");

// Set focus to the first element
$('#submission_dialog').on('shown.bs.modal', function () {$('#InputIsActive').focus()})

$("#submissiones").smart_listing().fadeLoaded();
