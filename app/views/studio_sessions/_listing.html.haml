.row
  .col-sm-12
    .card.card-outline-primary.table-card
      .table-responsive
        %table.table.table-hover#sortable
          %thead.thead-inverse
            %th
            %th ID
            %th Type
            %th Coach
            %th Day of Week
            %th Start Time
            %th Length
            %th Updated
            %th
          %tbody#studio_sessions
            - smart_listing.collection.each do |studio_session|
              = render studio_session
        %br
%br
.row
  .col-sm-12
    .pull-right
      = smart_listing.paginate
      = smart_listing.pagination_per_page_links
