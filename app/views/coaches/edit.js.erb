// Render the form
$('.modal-body').html('<%= j render "coaches/form" %>');

// Show the dynamic dialog
$('#coach_dialog').modal("show");

// Set focus to the first element
$('#coach_dialog').on('shown.bs.modal', function () {$('#InputName').focus()})

$("#coaches").smart_listing().fadeLoaded();
