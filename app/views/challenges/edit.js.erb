// Render the form
$('.modal-body').html('<%= j render "challenges/form" %>');

// Show the dynamic dialog
$('#challenge_dialog').modal("show");

// Set focus to the first element
$('#challenge_dialog').on('shown.bs.modal', function () {$('#InputName').focus()})

$("#challenges").smart_listing().fadeLoaded();
