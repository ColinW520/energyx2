// Render the form
$('.modal-body').html('<%= j render "events/form" %>');

// Show the dynamic dialog
$('#event_dialog').modal("show");

// Set focus to the first element
$('#event_dialog').on('shown.bs.modal', function () {$('#InputName').focus()})

$("#events").smart_listing().fadeLoaded();
