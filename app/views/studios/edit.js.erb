// Render the form
$('.modal-body').html('<%= j render "studios/form" %>');

// Show the dynamic dialog
$('#studio_dialog').modal("show");

// Set focus to the first element
$('#studio_dialog').on('shown.bs.modal', function () {$('#InputName').focus()})

$("#studios").smart_listing().fadeLoaded();
